import React from "react";
import AsNavForSlider from "../../AsNavForSlider/AsNavForSlider";

export const SliderPopup = ({ sliderImages }) => {
  return <AsNavForSlider sliderImages={sliderImages} />;
};
